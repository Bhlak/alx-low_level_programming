#include "main.h"

/**
 * _abs - Finds the absolute value of a number
 *
 * @n: Number to be checked
 *
 * Return: Absolute value of the number
 */

int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
